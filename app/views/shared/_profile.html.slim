.card
  .card-header プロフィール
  .card-body
    h3.card-title= link_to profile.name, profile
    - if profile.admin?
      p.small.form-text.text-muted 管理者
    
    p.card-text.my-4
      | 所持ポイント：
      span#profile_point= profile.point.to_s(:delimited)
      |  pt

    .mb-2
  .card-footer
    - if current_page?(profile)
      .text-left
        = link_to edit_profile_path(profile) do
          = fa_icon 'edit', class: 'icon_link'
          = " 編集"
    - if profile.id == current_profile.id && !current_page?(rewords_path)
      .text-left
        = link_to rewords_path do
          = fa_icon 'gift', class: 'icon_link'
          = " ポイント交換所"
    .text-left
      = link_to profiles_path do
        = fa_icon 'group', class: 'icon_link'
        = " プロフィール切替"